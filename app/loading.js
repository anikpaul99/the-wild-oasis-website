import Spinner from "@/app/_components/Spinner";

/**
 * A global loader. Will apply to any page.
 * @returns {JSX.Element}
 * @author Anik Paul
 */
export default function Loading() {
  return <Spinner />;
}
